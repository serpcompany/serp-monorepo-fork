{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".nuxt/**", ".output/**", "dist/**"]
    },
    "test": {},
    "test:watch": {},
    "lint": {},
    "dev": {
      "cache": true,
      "persistent": true
    }
  }
}
